[project]
name = "dashhouse-infrastructure-pulumi"
version = "0.1.0"
description = "Pulumi infrastructure for Dashhouse"
requires-python = ">=3.13"
dependencies = [
    "pip",
    "pulumi",
    "pulumi-aws",
    "pulumi-docker",
    "pulumi-synced-folder",
]
